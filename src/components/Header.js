import React from 'react';

const Header = () => {
  return (
    <header style={{ padding: '10px', background: '#007BFF', color: 'white' }}>
      <h1>My React App</h1>
    </header>
  );
};

export default Header;